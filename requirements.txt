# To update these, install `pip install piprot` and then run `piprot -o` and
# manually update the versions.

Django==3.0rc1
uvicorn==0.10.8
python-decouple==3.3
requests[security]==2.22.0
requests-cache==0.5.2
flake8==3.7.9
black==19.10b0
