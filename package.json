{
  "name": "gitbusy",
  "license": "MPL-2.0",
  "engines": {
    "node": ">=12",
    "yarn": ">=1.19"
  },
  "scripts": {
    "build": "ls -l node_modules && cd frontend && yarn install && ls -l node_modules/.bin && yarn build"
  }
}
